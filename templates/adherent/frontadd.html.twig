{% extends 'base.html.twig' %}

{% block body %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    <div class="card-body">
        <h5 class="card-title">add adherent</h5>

        {{form_start(form,{'attr' :{'novalidate':'novalidate'}})}}

        {{ form_end(form) }}
    </div>
{% endblock %}